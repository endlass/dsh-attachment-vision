# Copy to ~/.dsh/.env (or .credentials.yaml) and fill in your key.
# dsh-attachment-vision resolves credentials via the dsh credentials service.

QWEN_VL_API_KEY=sk-xxx
# QWEN_VL_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# QWEN_VL_MODEL=qwen3-vl-flash
# DSH_ATTACHMENT_VISION_DEBUG=1
